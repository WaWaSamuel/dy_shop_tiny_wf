# Frontend env vars (Vite). All vars must be prefixed with VITE_.
# By default the app talks to the backend through the Vite dev-server proxy
# (see vite.config.ts: "/api" -> http://localhost:8000), so no var is required
# for local development. Override the API base URL here if needed.

# VITE_API_BASE_URL=/api
